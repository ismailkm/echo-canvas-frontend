import { Sparkles, Volume2, VolumeX, Paintbrush, Mic } from 'lucide-react';

export const SparklesIcon = Sparkles;
export const MuteIcon = VolumeX;
export const UnmuteIcon = Volume2;
export const MicIcon = Mic;
export const PaintbrushIcon = Paintbrush;

// Add other icons here as the app grows